import { PesquisaPrestadorPipe } from './pesquisa-prestador.pipe';

describe('PesquisaPrestadorPipe', () => {
  it('create an instance', () => {
    const pipe = new PesquisaPrestadorPipe();
    expect(pipe).toBeTruthy();
  });
});
